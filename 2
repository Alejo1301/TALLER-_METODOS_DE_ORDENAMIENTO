# Ordenación de nombres por longitud: Ordena una lista de nombres según la cantidad de
# caracteres utilizando ordenamiento burbuja.


def burbuja_nombres(nombres):
    n = len(nombres)
    for i in range(n-1):
        for j in range(n-i-1):
            if len(nombres[j]) > len(nombres[j+1]):
                nombres[j], nombres[j+1] = nombres[j+1], nombres[j]
    return nombres


nombres = ["Raul", "Fabiola", "Wendy", "Leidy", "Yuliana", "Oscar", "Ariadna"]
print("Nombres originales:", nombres)
print("Nombres ordenados por longitud:", burbuja_nombres(nombres))


#Este código define una función burbuja_nombres que toma una lista de nombres como entrada y la ordena según la longitud de los nombres utilizando el algoritmo de ordenamiento por burbuja.

